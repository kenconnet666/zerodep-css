# 作者 API 与隐式绑定实施记录

日期：2026-09-26。范围来自[本阶段计划](next-phase-authoring-and-bindings.md)。运行时 CSS 保留，编译器只为可识别的响应式值生成绑定，不要求全项目预生成类名。

## 已落实

- 声明字符串不变；`cx`、`className`、`keyframes`、命名可更新 / 移除的 `globalCss` 已接入浏览器、Node SSR 和恢复清单。
- 502 属性生成器增加长度家族、百分比、时间、角度、rgb/hsl、calc/min/max/clamp；关键字仍为 12,586 个。不增加公开 var 方法。
- Vue `<script setup>` 与 Svelte 实例脚本插件；多参数、复杂 raw 表达式、列表作用域、cx 组合、动态帧值及全局块更新。
- Nuxt 默认集成插件；SvelteKit 使用 Svelte Vite 插件和现有宿主 hooks。Svelte 绑定最低需要 5.20 的 `$props.id()`，本轮固定版本为 5.57.0。
- 私有变量用 CSSOM 值规则更新，普通 `css` 仍输出一个类名字符串；允许把 class 传给未参与转换的子组件。
- 更宽的完整 UTF-16 哈希，miss 只计算一次，命中不计算；保留碰撞诊断。旧复现碰撞样本分离，30 万条顺序样本未发现新碰撞，不代表数学上无碰撞。
- nonce / 插入位置、失联主标签和变量标签重建、显式宿主清理、统计信息。Kit 自带两个固定内联样式需要精确 CSP 哈希许可，未放宽任意内联样式。

公开写法与回退边界见 [API](../docs/author-api.md)、[隐式绑定](../docs/implicit-bindings.md)、[元框架接入](../docs/metaframeworks.md)。未知字符串加工、用户覆写、派生 css 调用、slot/snippet 参数等保留原有求值；不会把一次性 const 初始化回退宣传成自动响应。

## 性能诊断

Windows、Node 24.18.0、Chrome 153.0.8010.54；Vue 3.5.43、Svelte 5.57.0、Emotion CSS 11.13.5。每个用例新页面、200 行、20 次连续值更新，三轮轮换执行顺序。时间包含框架刷新和每轮布局读取，下面为中位数；不是 `css()` 单函数微基准，也不代表真实页面整体快相同比例。

| 框架 / 路径       | 首次挂载 ms | 20 次更新 ms | 初始 → 最终规则数 |
| ----------------- | ----------: | -----------: | ----------------: |
| Vue 隐式绑定      |        29.1 |         81.7 |         400 → 400 |
| Vue 普通运行时    |        23.9 |        105.0 |       200 → 4,200 |
| Vue 手工变量      |        21.5 |         56.7 |             1 → 1 |
| Vue Emotion       |        27.8 |        127.1 |       200 → 4,200 |
| Svelte 隐式绑定   |        34.0 |         76.8 |         400 → 400 |
| Svelte 普通运行时 |        23.9 |        102.6 |       200 → 4,200 |
| Svelte 手工变量   |        23.2 |         52.9 |             1 → 1 |
| Svelte Emotion    |        31.2 |        128.7 |       200 → 4,200 |

这组连续新值用例中，隐式绑定更新比普通运行时少约 22% / 25% 的耗时，比 Emotion 少约 36% / 40%；仍是手工变量的约 1.44 / 1.45 倍。首次挂载比普通运行时更贵，因为每行建立私有变量记录和目标类。它的明确收益是持续更新不再积累新规则，同时保持自然作者写法和 class 字符串传递。

手工变量对照使用一条共享类和框架内联 style 绑定；隐式路径为每行一条类规则加一条值规则，能够用于禁止任意内联 style 的 CSP。这不是相同机制，也不据此承诺“等于原生 CSS”。有限状态仍建议普通 if/switch 选择已有类，没必要全部变成动态绑定。

本轮依据测量避免了更新值时重复写 `selectorText`，保留已解析的选择器；内部帧用按调用点 / 列表 key 的缓存保存身份，变量组名字只在创建时哈希。不增加 Worker、SIMD、自定义 CSS 解析器或一层通用值对象。后续优化优先测实例和列表创建成本，不应为追逐单个比例破坏可维护性。

原始样本：[implicit-bindings.json](string-css-probe/results/implicit-bindings.json)。复跑：`pnpm --dir .research/string-css-probe probe:bindings`。CI 只验证规则数和计算样式，时间没有硬阈值。

## 验证与交付边界

本地按改动范围完成 `pnpm build`、`pnpm check`、core 运行时 / 绑定协议测试、作者方法测试，以及 Vue/Svelte 客户端与恢复、真实 Nuxt/Kit 生产构建、Node SSR、静态部署和 CSP 交互。类型检查没有修改全局工具或 Codex 配置。

CI 对 Windows / Ubuntu 做完整类型、生成一致性、格式和 LSP 验证；Ubuntu 额外覆盖包导出、作者写法、浏览器、SSR、恢复、共享示例和元框架。每个阶段提交已推送，实际是否通过以对应提交的 Actions 结果为准，不能用本地通过代替完整远程验收。

核心接入提交 `61d1b63` 的[完整远程矩阵](https://github.com/kenconnet666/zerodep-css/actions/runs/36224589418)已通过。本文之后的收尾提交仍需读取其自身 CI 结果。

仍不在本轮范围：公开 npm 发布、边缘平台、流式 SSR、Shadow DOM 和跨独立 SSR 应用的岛屿合并。包保持 private。无限新增 key 的单组件会持有其模板帧缓存直到卸载；动态结构继续由框架运行时重算。这里保留明确边界，避免用复杂的自动推断或全页面观察器掩盖生命周期问题。
