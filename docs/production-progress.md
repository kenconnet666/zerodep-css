# 生产完善执行记录

范围：运行时 CSS + 显式 bx，Vue 3.5 / Svelte 5、Nuxt 4 / SvelteKit 2 的 Node SSR 与静态部署。五包保持 private；发布、流式 SSR、边缘环境和 Shadow DOM 不在本轮承诺内。

## 阶段与验收

- [ ] 1. 修复 CI、全局块绑定缓存、格式化覆写语义，统一作者 API 契约。
- [ ] 2. 审核并完善 bx 的模板、列表、派生值、插槽、snippet 和清理边界。
- [ ] 3. 统一条件/数组片段输入，颜色方法接纳 bx，补充高频能力。
- [ ] 4. 审核主题继承、组件转发、SSR 初始值与调试体验。
- [ ] 5. 按职责整理编译器、生成器、正式测试和文档，删除旧转换残留。
- [ ] 6. 并行 CI 覆盖三浏览器、框架生命周期、两个元框架及包消费。
- [ ] 7. 对照性能与资源计数，收敛文档、兼容范围和交接记录。

## 已完成的修复

- ccb0c67：修复元框架示例完整交互的规则计数。普通运行时尺寸分支和新宽度各新增一条规则；显式 bx 的稳定规则数量单独验收。原失败运行：36250115653（d541ae9）。

## 交付纪律

本地只运行本阶段焦点检查。每阶段中文提交并推送；推送后继续工作，下次提交前读取上一轮 CI 并修复。完整验收结果记录在本文件；不能把已推送等同于已通过验收。时间比较用于定位热点，不设置无依据的性能硬阈值。

## 最近验收

- ccb0c67：完整 CI 36251241296 通过。
- dd37300：并行 CI 36252095705 全部通过，包含三浏览器、两个框架 HMR、两个元框架与四组性能任务。
- 当前实现：命名全局块不复用调用结果缓存；动画/全局块统一条件数组片段；动态属性格式化统一；颜色通道支持字符串和 bx，增加 oklch/oklab、_placeholder。

- 223765c 的三浏览器、基础、元框架、生命周期与绑定性能验收通过；author 性能任务发现静态研究探针仍导入旧元数据入口，已修正到 selector-shortcuts.ts。
- 本轮扩大 bx 验收：命名 getter、可写 computed、scoped slot；常量省订阅；异常回滚和释放后防止再次写入。

- 整理：编译转换、模板缓存分析、源码定位分开维护；共享测试工具迁至 test/tools；删除 8 个 legacy 文件和 4 份被现行文档替代的阶段计划。Windows 本机绝对依赖链接已用锁文件离线重建。
- 核心源码新增直接执行与 LCOV 覆盖率报告，14 项运行时用例通过：行覆盖 94.27%、分支覆盖 85.71%，仅指报告中列出的核心运行时文件。
