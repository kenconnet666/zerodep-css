# 内置亮暗主题与追加审计

用户于 2026-09-23 追加：基础生产化完成后，提供系统亮色/暗色两套主题，进一步扩大审计与测试，发现问题继续修复。0.1.0 基线 9d07fce 的完整 CI 35764976781 已成功；本文件对应后续仍在执行的目标。

用户随后再次要求重新审视、整理和优化整个实现，扩大完整审计与测试。该要求包含架构、目录/API、一致性和资源预算，不仅是增加预设 happy-path 用例。

## 内置预设

- 提供 lightTheme 和 darkTheme 共两套语义主题，暗色通过亮色预设继承，变量身份和 schema 一致。
- 通过独立 themes 子路径按需加载，不让仅使用 core/Css/cssVar 的应用初始化预设或增加默认入口体积。
- Css 基类继续只提供系统 CSS；可选 ThemeCss 子类提供语义颜色及常用 token 的作者便利，用户仍可以继续继承。
- 配色包含画布、表面、文本层级、主色/辅助色、成功/警告/错误/信息、边框与焦点，并给出两套阴影和共享尺度。
- 文本与背景、强调色与其前景色进行独立 WCAG 对比度计算；真实组件验证亮暗同时存在、动态切换、局部覆盖、SSR/hydration 和三引擎结果。

## 追加验收

1. 全部预设 token 类型、名称冲突、值域、冻结/继承、覆盖重置与变量身份。
2. 多状态动态值的编译/运行时对照、空值/CSS-wide、重复声明、嵌套/守卫、不同实例与跨目标边界。
3. 缓存预算与大输入、失败重试、资源事务、销毁/重建、并行 SSR、主题同名冲突与外部对象变更。
4. 可访问性对比度、可读默认颜色、亮暗混合显示，以及快照和实际计算样式。
5. 独立安装、公开入口/类型、源码地图、默认包体积和全套 CI；不以新增几个 happy-path 测试替代审计。

完整长测试继续交给 CI/CD，本地运行类型/构建和新增缺陷对应的关键测试。发现回归时先补独立复现，再修复；不保留已知缺陷来满足预定结束时间。

## 初步发现与进度

- 亮暗数据、继承身份、可选 ThemeCss 与三包 themes 子路径已实现；根类型/构建、全 token getter 和独立对比度测试通过，默认 cssVar 入口仍为 3,610 minified / 1,333 gzip 字节。
- 新预设 getter 的自动声明推断曾产生 118,213 字节；已用基础属性索引类型明确返回合同，降至 3,927 字节，保留所有补全和类型负例。
- 新增 R8：严格 style-src-attr 'none' 下，SSR 元素变量属性被 CSP 阻止，而客户端 CSSOM 更新仍能生效。已增加 bindings:'runtime'，保留静态准备和源码诊断；真实 Vue/Svelte 的 nonce、首屏、hydration、更新与清理本地通过，完整三引擎验证交给 CI。
- 新增 R9：开发诊断曾在已回退的复杂循环/slot 中误包装局部同名 css 函数。已共用 TS 绑定模式解析，覆盖 Vue 解构循环/slot 与 Svelte each/await/legacy let；27 项编译测试和真实开发诊断 SSR/hydration 回归通过，局部 css(3) 保持原参数。
- 追加缓存大输入审查：条目数量有界不等于字符串总量有界，需要检查大型 CSS 值是否被长期保留，并在不拒绝合法 CSS 的前提下收紧缓存预算。

已收紧字符串缓存为 128 项/65,536 UTF-16 字符双预算；超大合法值跳过缓存，更新为超限值时清掉旧值以免返回过期结果。大输入、Unicode、替换、FIFO 淘汰和实际格式化器测试通过。

- 新增 R10：raw('banana') 在直接声明时保留前置红色，但盲目变量化后变为继承色。已按属性语法判断可证明值，未知/不合法/无法静态判断的值保留直接声明；同时让绑定解析器与运行时一致地解析 var 的 fallback。真实两框架多状态对照覆盖颜色、CSS-wide、空值、未知值、CSS 变量、小数尺寸、负 margin、padding tuple 和 opacity。
- R10 进一步复核：CSS 语法表接受并不代表浏览器接受，例如 Chrome 的 display:run-in、ruby-base-container 和 width:fit-content(10px)。变量化增加保守基础值策略，较新单位、复杂数学和未确认关键字保留直接声明；对照增加 display 和 width fallback，避免把语法校验误当浏览器支持证明。
- 新增 R11：合法的 s.constructor.name 读取曾因元数据继承 Object 原型而让优化器报错；现只读取元数据自有属性，不是 CSS 声明的表达式安全回退。
- 亮暗真实面板已增加 SSR/hydration、20 次切换、兄弟隔离、焦点、全部便利成员映射与截图；切换不新增内容规则。预设声明文件额外设置 8 KiB 预算，防止重复展开系统 CSS 类型。

当前对照在每个框架实际执行 14 种状态的两轮循环，并核对颜色、尺寸、display、padding、margin、opacity；包含本地浏览器明确不支持而语法表接受的值。预设截图已人工查看，本地检查通过后继续由三引擎完整 CI 验收。

另实测发现配置边界问题：layers:'base' 曾被拆成四个层、target:false 曾静默进入服务端模式、调用者修改 nonce 会影响已有 SSR runtime。已用严格输入校验和创建时快照修复，补充非容器/非数组、原对象变更、getter 单次读取回归；本地配置与 ShadowRoot 场景通过。
